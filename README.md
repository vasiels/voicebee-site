# VoiceBee Releases

This repo hosts **GitHub Releases** for the VoiceBee desktop app (macOS & Windows).

- `latest.json` — used by the Tauri updater to check for new versions
- DMG / EXE installers and updater artifacts are attached to each release
